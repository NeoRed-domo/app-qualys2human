"""Upgrade package — signature verification, chunked upload, version comparison.

APP_VERSION is defined here (not in main.py) to avoid circular imports
when the upgrade API router needs it.
"""

APP_VERSION = "1.1.13.6"
